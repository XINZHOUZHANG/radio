[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "remote-radio-server"
version = "0.1.0"
description = "Hamlib control plane for remote radio"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
